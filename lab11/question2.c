#include <stdio.h>
#include <string.h>
int main(){
	char line[100];
	printf("Enter a string:");
	fgets(line,sizeof(line),stdin);
	for(int i=0;line[i]!='\0';++i);
	while(!(line[i]>='a'&& line[i]<=z)&& !(line[i]>=A && lin[i]<=Z)){
		for (j=1)
	}
	
}